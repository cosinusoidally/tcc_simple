./mk_clean
cd artifacts
cd artifacts
cp -r ../orig .
cd orig

set -xe

make

mkdir asm

for i in *\.c ; do 
  j=$(basename $i .c)
  ./chibicc -S $j.c -o asm/$j.s
done

cd asm
sha256sum -c ../../../sums_orig
